import {
    SET_FOOD,
    SET_PLACES,
    SET_RATED_FOOD,
    SET_SEARCHED_FOOD,
    SET_SEARCHED_PLACES,
} from './Action'

function searchReducer(state = [], action){
    switch(action.type){
    }
}