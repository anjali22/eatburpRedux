import React from 'react';
import {
  ActivityIndicator,
  Image,
  InteractionManager,
  Platform,
  ScrollView,
  StyleSheet,
  TouchableHighlight,
  TouchableOpacity,
  TouchableWithoutFeedback,
  View,
  Text,
} from 'react-native';  

export class MenuCard extends React.Component{
  render = () => {
    return (
        <View >
             <Text> Inside MenuCard  </Text>
        </View>
      )
}
}